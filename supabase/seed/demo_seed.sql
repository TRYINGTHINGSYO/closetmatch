-- Development seed placeholders (no real user images)
-- Apply after creating a local auth user, then replace :user_id

-- See services/storage/demo-data.ts for the canonical in-app seed used by the Expo demo mode.
-- This SQL seed is optional for Supabase Studio / local Postgres.

/*
Example (run in SQL editor after signup):

-- clothing + outfits should be inserted with the authenticated user's UUID
-- Prefer using the app "Import sample wardrobe" action during onboarding.
*/
